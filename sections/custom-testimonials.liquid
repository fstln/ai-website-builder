{%- comment -%}
  Custom Testimonials Section
  - Displays customer testimonials in a 3-column grid.
  - Each testimonial is a block with settings for author, image, and quote.
  - Includes a subtle hover effect for interactivity.
{%- endcomment -%}

<section class="bg-slate-950 py-20 sm:py-24">
    <div class="container mx-auto px-6 lg:px-8">
        <!-- Section Header -->
        <div class="max-w-2xl mx-auto text-center mb-12">
            {%- if section.settings.title != blank -%}
            <h2 class="text-4xl font-bold tracking-tight text-white sm:text-5xl">{{ section.settings.title }}</h2>
            {%- endif -%}
            {%- if section.settings.subtitle != blank -%}
            <p class="mt-4 text-lg leading-8 text-gray-400">{{ section.settings.subtitle }}</p>
            {%- endif -%}
        </div>

        <!-- Testimonials Grid -->
        {%- if section.blocks.size > 0 -%}
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {%- for block in section.blocks -%}
            <div class="bg-slate-900/50 border border-white/10 rounded-3xl p-8 space-y-6 transform transition-transform duration-300 hover:-translate-y-2">
                <div class="flex items-center gap-4">
                    {%- if block.settings.author_image != blank -%}
                        <img class="h-12 w-12 rounded-full object-cover" 
                             src="{{ block.settings.author_image | image_url: width: 100, height: 100, crop: 'center' }}" 
                             alt="{{ block.settings.author_image.alt | default: block.settings.author_name | escape }}"
                             width="100" height="100" loading="lazy">
                    {%- else -%}
                        <img class="h-12 w-12 rounded-full object-cover" 
                             src="https://placehold.co/100x100/334155/FFFFFF?text={{ block.settings.author_name | slice: 0 | upcase }}" 
                             alt="Placeholder for {{ block.settings.author_name | escape }}"
                             width="100" height="100" loading="lazy">
                    {%- endif -%}
                    <div>
                        <h4 class="text-lg font-semibold text-white">{{ block.settings.author_name }}</h4>
                        <p class="text-sm text-indigo-400">{{ block.settings.author_title }}</p>
                    </div>
                </div>
                <div class="relative">
                    <i class="ri-double-quotes-l text-5xl text-slate-700 absolute -top-4 -left-4"></i>
                    <div class="relative text-lg text-gray-300 leading-relaxed">
                        {{ block.settings.quote }}
                    </div>
                </div>
            </div>
            {%- endfor -%}
        </div>
        {%- endif -%}
    </div>
</section>

{% schema %}
{
  "name": "C: Testimonials",
  "tag": "section",
  "class": "custom-testimonials-section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "What Our Fans Say"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "We're not the only ones who love Oladance. See what our community thinks."
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "limit": 6,
      "settings": [
        {
          "type": "image_picker",
          "id": "author_image",
          "label": "Author Image"
        },
        {
          "type": "text",
          "id": "author_name",
          "label": "Author Name",
          "default": "Sarah K."
        },
        {
          "type": "text",
          "id": "author_title",
          "label": "Author Title / Subtitle",
          "default": "Verified Buyer"
        },
        {
          "type": "richtext",
          "id": "quote",
          "label": "Quote",
          "default": "<p>Finally, earbuds that don't hurt my ears! I can wear these all day and completely forget they're there. The comfort is unreal.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "C: Testimonials",
      "blocks": [
        { "type": "testimonial" },
        { 
          "type": "testimonial",
          "settings": {
            "author_name": "Mike R.",
            "author_title": "Audio Enthusiast",
            "quote": "<p>The sound quality is surprisingly good for open-ear. I was skeptical, but the bass is really there. It's rich and full without sealing me off from the world.</p>"
          }
        },
        { 
          "type": "testimonial",
          "settings": {
            "author_name": "David J.",
            "author_title": "Runner & Cyclist",
            "quote": "<p>Perfect for my runs. They stay in place no matter how much I move, and I can still hear traffic. It's a game-changer for safety and awareness.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
