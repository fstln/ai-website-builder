{%- comment -%}
  This section explains how to use the product in simple, numbered steps.

  **Best Practices:**
  - PM: Break down the process into 3-4 simple, action-oriented steps.
  - Designer: Use large numbers, icons, and clear typography to guide the user.
  - Architect: Use repeater blocks and forloop.index for automatic numbering.
{%- endcomment -%}

{%- liquid
  assign section_dom_id = 'custom-how-it-works-' | append: section.id
-%}

<div class="color-scheme color-{{ section.settings.color_scheme | default: 'scheme-1' }} isolate">
  <section
    id="{{ section_dom_id }}"
    class="bg-background text-foreground py-12 sm:py-16 lg:py-20"
    aria-labelledby="how-it-works-title-{{ section.id }}"
  >
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center">
        {%- if section.settings.title != blank -%}
          <h2 id="how-it-works-title-{{ section.id }}" class="text-3xl sm:text-4xl font-bold tracking-tight text-foreground">
            {{ section.settings.title | escape }}
          </h2>
        {%- endif -%}
        {%- if section.settings.subtitle != blank -%}
          <p class="mt-4 text-base leading-relaxed text-muted">
            {{ section.settings.subtitle }}
          </p>
        {%- endif -%}
      </div>

      {%- if section.blocks.size > 0 -%}
        <div class="mt-12 sm:mt-16 grid grid-cols-1 gap-x-8 gap-y-12 md:grid-cols-3">
          {%- for block in section.blocks -%}
            <div
              class="flex flex-col items-center text-center"
              {{ block.shopify_attributes }}
            >
              <div class="flex items-center justify-center h-16 w-16 rounded-full bg-primary text-primary-foreground mb-6">
                <span class="text-2xl font-bold">{{ forloop.index }}</span>
              </div>
              {%- if block.settings.heading != blank -%}
                <h3 class="text-xl font-semibold text-foreground">
                  {{ block.settings.heading | escape }}
                </h3>
              {%- endif -%}
              {%- if block.settings.description != blank -%}
                <p class="mt-2 text-base text-muted">
                  {{ block.settings.description }}
                </p>
              {%- endif -%}
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>
  </section>
</div>

{% schema %}
{
  "name": "How It Works",
  "tag": "section",
  "class": "custom-how-it-works-section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Get Started in 3 Easy Steps"
    },
    {
      "type": "richtext",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "<p>Using our product is simple and intuitive. Here's how to get up and running in minutes.</p>"
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "limit": 3,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Step One"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>A brief description of the first step. Keep it concise and easy to understand.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "How It Works",
      "blocks": [
        { "type": "step" },
        { "type": "step" },
        { "type": "step" }
      ]
    }
  ]
}
{% endschema %}
