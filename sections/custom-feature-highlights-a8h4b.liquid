{%- comment -%}
  This section is designed to showcase key product features with an alternating image-and-text layout.
  It uses CSS Grid to create a modern, asymmetrical layout that's responsive by default.

  **Best Practices:**
  - PM: Clearly define the user benefit for each feature.
  - Designer: Use high-quality, lifestyle imagery. Ensure bold typography and generous spacing.
  - Architect: Use repeater blocks for features. Ensure robust Liquid checks for optional images.
{%- endcomment -%}

{%- liquid
  assign section_dom_id = 'custom-feature-highlights-' | append: section.id
-%}

<div class="color-scheme color-{{ section.settings.color_scheme | default: 'scheme-1' }} isolate">
  <section
    id="{{ section_dom_id }}"
    class="bg-background text-foreground py-12 sm:py-16 lg:py-20"
    aria-labelledby="feature-highlights-title-{{ section.id }}"
  >
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center">
        {%- if section.settings.eyebrow != blank -%}
          <p class="text-sm font-medium uppercase tracking-[0.2em] text-muted mb-2">
            {{ section.settings.eyebrow | escape }}
          </p>
        {%- endif -%}
        {%- if section.settings.title != blank -%}
          <h2 id="feature-highlights-title-{{ section.id }}" class="text-3xl sm:text-4xl font-bold tracking-tight text-foreground">
            {{ section.settings.title | escape }}
          </h2>
        {%- endif -%}
        {%- if section.settings.subtitle != blank -%}
          <p class="mt-4 text-base leading-relaxed text-muted">
            {{ section.settings.subtitle }}
          </p>
        {%- endif -%}
      </div>

      {%- if section.blocks.size > 0 -%}
        <div class="mt-12 sm:mt-16 space-y-16">
          {%- for block in section.blocks -%}
            <div
              class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 items-center"
              {{ block.shopify_attributes }}
            >
              {%- liquid
                assign is_odd = forloop.index | modulo: 2
                if is_odd == 1
                  assign image_order = 'md:order-last'
                else
                  assign image_order = ''
                endif
              -%}
              <div class="{% if block.settings.image != blank %}{{ image_order }}{% endif %}">
                {%- if block.settings.heading != blank -%}
                  <h3 class="text-2xl font-semibold text-foreground">
                    {{ block.settings.heading | escape }}
                  </h3>
                {%- endif -%}
                {%- if block.settings.description != blank -%}
                  <div class="mt-4 text-base leading-relaxed text-muted">
                    {{ block.settings.description }}
                  </div>
                {%- endif -%}
              </div>

              <div>
                {%- if block.settings.image != blank -%}
                  {{ block.settings.image | image_url: width: 1200 | image_tag:
                    loading: 'lazy',
                    widths: '300, 400, 600, 800, 1000, 1200',
                    class: 'w-full h-auto rounded-xl shadow-md object-cover aspect-[4/3]'
                  }}
                {%- else -%}
                  <div class="w-full bg-surface-muted rounded-xl shadow-md aspect-[4/3] flex items-center justify-center">
                     <img src="https://placehold.co/800x600/f0f0f0/ccc?text=Feature+Image" alt="Feature highlight placeholder" class="w-full h-auto rounded-xl object-cover">
                  </div>
                {%- endif -%}
              </div>
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>
  </section>
</div>

{% schema %}
{
  "name": "Feature Highlights",
  "tag": "section",
  "class": "custom-feature-highlights-section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-2"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow Text",
      "default": "Key Features"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Why OWS Pro is Different"
    },
    {
      "type": "richtext",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "<p>Discover the innovative technology that sets our product apart from the rest.</p>"
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature Item",
      "limit": 4,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Feature Title"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>Use this space to describe the feature in detail. Explain the benefit to the user and how it solves a problem.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Feature Highlights",
      "blocks": [
        { "type": "feature" },
        { "type": "feature" },
        { "type": "feature" }
      ]
    }
  ]
}
{% endschema %}
