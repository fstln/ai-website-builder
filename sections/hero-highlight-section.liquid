{%- comment -%}
  Reusable hero highlight section featuring a split layout with optional CTA.
  - Designer: Emphasized bold typography and generous spacing.
  - Architect: Built with utility-first CSS and a fully configurable schema.
{%- endcomment -%}

{%- liquid
  assign fallback_section_id = 'hero-highlight-section-' | append: section.index
  assign section_id = section.settings.section_id | default: fallback_section_id
  assign heading = section.settings.heading
  assign subheading = section.settings.subheading
  assign button_label = section.settings.button_label
  assign button_link = section.settings.button_link
  assign image = section.settings.image
  assign image_position = section.settings.image_position | default: 'right'
  assign text_classes = 'order-1'
  assign image_classes = 'order-2'
  if image_position == 'left'
    assign text_classes = text_classes | append: ' md:order-2 lg:order-2'
    assign image_classes = image_classes | append: ' md:order-1 lg:order-1'
  else
    assign text_classes = text_classes | append: ' md:order-1 lg:order-1'
    assign image_classes = image_classes | append: ' md:order-2 lg:order-2'
  endif
-%}

<section
  id="{{ section_id | escape }}"
  class="color-scheme color-{{ section.settings.color_scheme }} bg-background overflow-hidden"
  data-section-id="{{ section.id }}"
>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-x-8 gap-y-12 py-16 sm:py-20 lg:py-24">
      <div class="{{ text_classes }} flex flex-col justify-center items-start text-left">
        <div class="max-w-lg">
          {%- if subheading != blank -%}
            <p class="text-base font-semibold leading-7 text-primary">{{ subheading }}</p>
          {%- endif -%}
          {%- if heading != blank -%}
            <h1 class="mt-2 text-4xl font-bold tracking-tight text-foreground sm:text-5xl lg:text-6xl font-heading">
              {{ heading }}
            </h1>
          {%- endif -%}
          <p class="mt-6 text-lg leading-8 text-muted">
            {{ section.settings.paragraph }}
          </p>
          {%- if button_label != blank and button_link != blank -%}
            <div class="mt-10">
              <a href="{{ button_link }}" class="btn btn-primary">
                {{ button_label }}
              </a>
            </div>
          {%- endif -%}
        </div>
      </div>
      <div class="{{ image_classes }} flex items-center justify-center">
        {%- if image != blank -%}
          <img
            src="{{ image | image_url: width: 1000 }}"
            alt="{{ image.alt | escape }}"
            width="1000"
            height="1000"
            class="w-full max-w-md md:max-w-none rounded-2xl object-cover"
            loading="eager"
          >
        {%- else -%}
          <img
            src="https://placehold.co/1000x1000/F3F4F6/333333?text=Hero+Image"
            alt="Hero placeholder image"
            width="1000"
            height="1000"
            class="w-full max-w-md md:max-w-none rounded-2xl object-cover"
            loading="eager"
          >
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero highlight",
  "tag": "section",
  "class": "hero-highlight-section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-2"
    },
    {
      "type": "text",
      "id": "section_id",
      "label": "Section ID",
      "info": "Used for anchor links. Defaults to hero-highlight-section-{index} when left blank."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Image Position",
      "default": "right",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "right", "label": "Right" }
      ]
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Featured product"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Bring your big story to the top of the page"
    },
    {
      "type": "richtext",
      "id": "paragraph",
      "label": "Paragraph",
      "default": "<p>Use this space to explain your value proposition, product benefit, or promotion with concise, compelling copy.</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    }
  ],
  "presets": [
    {
      "name": "Hero highlight"
    }
  ]
}
{% endschema %}
