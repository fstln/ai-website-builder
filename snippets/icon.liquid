{% comment %}
  Icon snippet using Remix Icon
  
  Usage:
  {% render 'icon', name: 'menu-line', size: 24 %}
  
  Parameters:
  - name: Remix Icon name (e.g., 'menu-line', 'search-line', 'shopping-cart-line')
  - size: Icon size in pixels (default: 24)
  - class: Additional CSS classes
{% endcomment %}

{% liquid
  assign icon_size = size | default: 24
  assign icon_class = class | default: ''
%}

<i class="ri-{{ name }} {{ icon_class }}" style="font-size: {{ icon_size }}px;" aria-hidden="true"></i>
