{% comment %}
  静态页面模板
{% endcomment %}

<div class="container-custom py-8">
  {% render 'breadcrumbs' %}
  
  <article class="page-content mt-8 max-w-3xl mx-auto">
    <h1 class="text-4xl font-bold mb-8">{{ page.title }}</h1>
    
    <div class="prose max-w-none">
      {{ page.content }}
    </div>
  </article>
</div>

